<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Jangan process actual files atau directories
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Rewrite ke index.php
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>